/**
 * Page backdrop (overscroll): matches `theme.colors.background` in hex for
 * the rare places that still read a literal canvas colour (SSR + analytics).
 */
export const HOME_PAGE_CANVAS_COLOR = '#FFF7F4' as const;
